<nav class="navigation">
    <div class="brand">BRAND</div>
    <input type="checkbox"/>
    <menuitems class="menu-items">
        <a href="/">Home</a>
        <a href="/about/">About</a>
        <a href="/blog/">Blog</a>
        <a href="/tags/">Tags</a>
    </menuitems>
    <useritems class="user-items">
        <button id="navigation-toggle" class="toggler"></button>
    </useritems>
</nav>

<style lang="scss">
    .navigation {
        height: 4rem;
        width: 100vw;
        position: fixed;
        top: 0;
        left: 0;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 24px;
        background-color: bisque;

        input {
            position: absolute;
            width: 32px;
            height: 32px;
            transform: translate(-100%,50%);
            right: 2rem;
            top: 50%;
            transform: translateY(-50%);
            z-index: 10;
            opacity: 0;
            cursor: pointer;
        }

        .brand {
            position: absolute;
            left: 1rem;
            top: 50%;
            transform: translateY(-50%);
        }

        input:not(:checked)+.menu-items {
            right: -30vw;
            transition: all 300ms ease-in-out;
        }

        input:checked+.menu-items {
            right: 0%;
            transition: all 350ms ease-in-out;
        }

        .user-items {

            position: absolute;
            right: 2rem;
            top: 50%;
            transform: translateY(-50%);
            --border: gray;

            &:hover {
                --border: black;
            }

            button {
                position: relative;
                top: 0;
                right: 0;
                border-top: 4px solid var(--border);
                border-bottom: 4px solid var(--border);
                width: 32px;
                height: 32px;
                visibility: hidden;

            &::after {
                content: "";
                position: absolute;
                inset: 0;
                border-bottom: 4px solid var(--border);
                transform: translateY(-40%);
            }

            @media(max-width: 767px) {
                visibility: visible;
            }
        }
        }

        .menu-items {
            display: flex;
            gap: 16px;
            font-weight: bolder;

            @media(max-width: 767px) {
                flex-direction: column;
                position: absolute;
                right: 0;
                top: 0;
                padding: 92px 2rem;
                background-color: bisque;
                width: 30vw;
                height: 100vh;
            }
        }
    }
</style>